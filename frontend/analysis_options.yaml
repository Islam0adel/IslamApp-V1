# ده ملف إعدادات التحذيرات - خليناه بسيط عشان ميطلعش أخطاء وهمية
analyzer:
  errors:
    # تجاهل الخطأ ده لو المكتبة مش موجودة
    uri_does_not_exist: ignore
    unused_import: warning

linter:
  rules:
    # هنا ممكن تضيف قواعد لو حبيت، حالياً هنخليها فاضية
    - avoid_print